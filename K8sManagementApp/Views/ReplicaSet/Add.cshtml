@using K8sManagementApp.ViewModels

@model ReplicaSetViewModel

@{
    ViewData["Title"] = "Add Replica Set";
}

<div class="layui-container">
    <form class="layui-form" action="/ReplicaSet/Add" method="post">
        <div class="layui-form-item">
            <label class="layui-form-label">Name</label>
            <div class="layui-input-block">
                <input type="text" name="Name" required lay-verify="required" placeholder="Enter replica set name" autocomplete="off" class="layui-input">
            </div>
        </div>
        <div class="layui-form-item">
            <label class="layui-form-label">Deployment</label>
            <div class="layui-input-block">
                <select name="DeploymentName" lay-verify="required">
                    @foreach (var deployment in Model.Deployments)
                    {
                        <option value="@deployment.Metadata.Name">@deployment.Metadata.Name</option>
                    }
                </select>
            </div>
        </div>
        <div class="layui-form-item">
            <label class="layui-form-label">Replicas</label>
            <div class="layui-input-block">
                <input type="number" name="Replicas" required lay-verify="required|number" placeholder="Enter number of replicas" autocomplete="off" class="layui-input">
            </div>
        </div>
        <div class="layui-form-item">
            <div class="layui-input-block">
                <button class="layui-btn" lay-submit lay-filter="form-submit">Create</button>
                <button type="reset" class="layui-btn layui-btn-primary">Reset</button>
            </div>
        </div>
    </form>
</div>

@section Scripts {
    <script>
        layui.use(['form', 'layer'], function () {
            var form = layui.form;
            var layer = layui.layer;

            // Submit Form
            form.on('submit(form-submit)', function (data) {
                $.ajax({
                    url: '/ReplicaSet/Add',
                    type: 'POST',
                    dataType: 'json',
                    data: JSON.stringify(data.field),
                    contentType: 'application/json',
                    success: function (result) {
                        if (result.success) {
                            layer.msg(result.message, { icon: 1 }, function () {
                                window.parent.location.reload();
                                var index = parent.layer.getFrameIndex(window.name);
                                parent.layer.close(index);
                            });
                        } else {
                            layer.msg(result.message, { icon: 2 });
                        }
                    },
                    error: function (xhr, status, error) {
                        layer.msg('An error occurred while submitting the form. Please try again.', { icon: 2 });
                    }
                });

                return false;
            });
        });
    </script>
}